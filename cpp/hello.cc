#include <iostream>
#include <stdio.h>

using namespace std;

int main (void) {

    printf("This is just a test to see if this works!\n");
    cout << "This is yet another test to see if my C++ skills are still there!" << std::endl;
    return 0;
}
